declare module Timeframes {
    type Timeframe = string | AbsoluteTimeframe | RelativeTimeframe;
    type Timezone = string | number;
    class AbsoluteTimeframe {
        start: Date;
        end: Date;
        constructor(start?: Date, end?: Date);
    }
    class RelativeTimeframe {
        isCurrent: boolean;
        length: number;
        period: string;
        constructor(isCurrent: boolean, length: number, period: string);
        direction(): string;
    }
    class TimeframeBuilder {
        from(from: Date): AbsoluteTimeframe;
        to(to: Date): AbsoluteTimeframe;
        between(start: Date, end: Date): AbsoluteTimeframe;
        current(length: number, period: string): RelativeTimeframe;
        last(length: number, period: string): RelativeTimeframe;
    }
}
export = Timeframes;
