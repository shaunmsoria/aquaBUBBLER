import Api = require('../api');
import Filters = require('./filters');
import Selects = require('./selects');
import Timeframes = require('./timeframes');
declare class QueryBuilder {
    build(selects: Selects.QuerySelects, filters: Filters.QueryFilters, groups: string[], timeframe: Timeframes.Timeframe, interval: string, timezone: Timeframes.Timezone): Api.Query;
    private _buildFilter(filters);
    private _buildTimeframe(timeframe);
    private _buildRelativeTimeframe(timeframe);
    private _buildAbsoluteTimeframe(timeframe);
}
export = QueryBuilder;
