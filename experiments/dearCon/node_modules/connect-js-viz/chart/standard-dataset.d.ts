import Api = require('../../core/api');
import Dataset = require('./dataset');
declare class StandardDataset implements Dataset.ChartDataset {
    private _metadata;
    private _selectLabelFormatter;
    private _groupValueFormatter;
    private _selectLabels;
    private _data;
    constructor(results: Api.QueryResultItem[], metadata: Api.Metadata, formatters: Dataset.Formatters);
    getLabels(): string[];
    getSelect(label: string): string;
    getData(): any;
    private _mapLabels(results);
    private _mapData(results);
    private _mapResult(result);
}
export = StandardDataset;
