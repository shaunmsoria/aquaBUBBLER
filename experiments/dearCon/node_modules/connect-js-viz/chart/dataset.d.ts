import Api = require('../../core/api');
declare module Dataset {
    function getLabels(labels: SelectLabel[]): any[];
    function getSelect(selectLabels: SelectLabel[], label: string): string;
    function getGroupPath(result: Api.QueryResultItem, groups: string[], formatter: (groupByName: string, groupValue: any) => string): string;
    interface Formatters {
        selectLabelFormatter: (value: string) => string;
        groupValueFormatter: (groupByName: string, groupValue: any) => string;
    }
    interface ChartDataset {
        getLabels(): string[];
        getSelect(label: string): string;
        getData(): any;
    }
    interface SelectLabel {
        select: string;
        label: string;
    }
}
export = Dataset;
