import Common = require('./visualization');
import Api = require('../core/api');
declare module ResultHandling {
    type LoadDataFunction = (results: Api.QueryResults, metadata: Api.Metadata, fullReload: boolean) => void;
    class ResultHandler {
        private _lastReloadTime;
        private _lastRequestProcessed;
        private _currentRequestCount;
        constructor();
        handleResult(resultsPromise: Q.IPromise<Api.QueryResults>, metadata: Api.Metadata, visualization: Common.Visualization, loadData: LoadDataFunction, fullReload: boolean): void;
    }
}
export = ResultHandling;
