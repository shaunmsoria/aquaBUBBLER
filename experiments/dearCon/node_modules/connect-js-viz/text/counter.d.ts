declare class Counter {
    private target;
    private countDown;
    private startTime;
    private timestamp;
    private remaining;
    private startValue;
    private endValue;
    private currentValue;
    private duration;
    private animationId;
    private animationTimestamp;
    private valueFormatter;
    constructor(target: HTMLElement, duration: number, valueFormatter: (value: number) => any);
    private printCurrentValue();
    private easeOutExpo(t, b, c, d);
    private count(timestamp, finished);
    stop(): void;
    update(endValue: number, finished?: () => void): void;
    setValue(newValue: any): void;
}
export = Counter;
