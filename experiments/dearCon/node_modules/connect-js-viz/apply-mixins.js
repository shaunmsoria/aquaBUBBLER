var _ = require('underscore');
function applyMixins(targetClass, mixinClass) {
    _.each(Object.getOwnPropertyNames(mixinClass.prototype), function (name) {
        targetClass.prototype[name] = mixinClass.prototype[name];
    });
}
module.exports = applyMixins;
