import Api = require('../core/api');
import Loader = require('./Loader');
export interface Visualization {
    targetElement: HTMLElement;
    loader: Loader;
    displayData(resultsPromise: Q.IPromise<Api.QueryResults>, metadata: Api.Metadata, showLoader?: boolean): void;
    clear(): void;
}
