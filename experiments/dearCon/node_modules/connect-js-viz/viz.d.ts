import Queries = require('../core/queries/queries');
import Config = require('./config');
declare module Viz {
    var DataVisualization: any;
    var Chart: any;
    var Gauge: any;
    var Text: any;
    var Table: any;
    class Visualizations {
        chart(query: Queries.ConnectQuery, targetElement: string | HTMLElement, chartOptions: Config.ChartOptions): any;
        text(query: Queries.ConnectQuery, targetElement: string | HTMLElement, textOptions: Config.TextOptions): any;
        table(query: Queries.ConnectQuery, targetElement: string | HTMLElement, tableOptions: Config.TableOptions): any;
        gauge(query: Queries.ConnectQuery, targetElement: string | HTMLElement, chartOptions: Config.ChartOptions): any;
    }
}
export = Viz;
