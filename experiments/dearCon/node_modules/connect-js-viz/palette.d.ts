import Config = require('./config');
declare module Palette {
    var defaultSwatch: string[];
    function getSwatch(keys: string[], colors?: Config.ChartColors | string[]): C3.ChartDataOption<string>;
}
export = Palette;
