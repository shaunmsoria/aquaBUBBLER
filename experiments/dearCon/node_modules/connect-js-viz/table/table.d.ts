import Common = require('../visualization');
import Config = require('../config');
import Api = require('../../core/api');
import Loader = require('../loader');
declare class Table implements Common.Visualization {
    targetElement: HTMLElement;
    loader: Loader;
    private _options;
    private _rendered;
    private _titleElement;
    private _tableWrapper;
    private _resultHandler;
    constructor(targetElement: string | HTMLElement, suppliedOptions: Config.VisualizationOptions);
    displayData(resultsPromise: Q.IPromise<Api.QueryResults>, metadata: Api.Metadata, fullReload?: boolean): void;
    private _loadData(results, metadata, fullReload);
    clear(): void;
    private _showTitle();
    private _renderTable(metadata);
}
export = Table;
