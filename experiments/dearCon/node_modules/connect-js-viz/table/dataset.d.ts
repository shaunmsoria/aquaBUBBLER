import Config = require('../config');
import Api = require('../../core/api');
export interface Cell {
    isNumeric?: boolean;
    isGrouped?: boolean;
    isInterval?: boolean;
}
export interface HeaderCell extends Cell {
    title: String;
}
export declare type HeaderRow = HeaderCell[];
export interface ContentCell extends Cell {
    rawValue: String | Number | Api.QueryResultInterval;
    displayedValue: String;
}
export declare type ContentRow = ContentCell[];
export declare class TableDataset {
    headerRow: HeaderRow;
    contentRows: ContentRow[];
    constructor(metadata: Api.Metadata, options: Config.VisualizationOptions, results?: Api.QueryResultItem[]);
    private _buildHeaderRow(metadata, options, results?);
    private _buildContentRows(metadata, options, results?);
}
